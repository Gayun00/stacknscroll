# StackNScroll - 앱 설계 문서

## 📱 프로젝트 개요

**한 문장 설명:**
"나중에 보려고 저장했지만 잊어버린 것들, 생각 없이 스크롤만 해도 다시 만나게 해주는 초저에너지 정보 피드."

**핵심 가치:**
- 저장만 하고 잊어버린 콘텐츠를 자연스럽게 재발견
- 의도적인 정리 없이도 스크롤만으로 다시 만남
- 최소한의 에너지로 저장된 정보 소비

---

## 🛠 기술 스택

### Frontend
- **React Native** (Expo)
  - TypeScript
  - React Navigation (화면 전환)
  - React Native Gesture Handler (스와이프)
  - React Native Reanimated (애니메이션)
  - Expo Share Extension (공유 기능)

### Backend
- **Firebase**
  - Authentication (사용자 인증)
  - Firestore (데이터베이스)
  - Cloud Storage (이미지/미디어)
  - Cloud Functions (링크 미리보기 생성)

### 기타
- **링크 미리보기:** Open Graph API / Unfurl.js
- **상태관리:** Zustand or React Context
- **스타일링:** Styled Components or NativeWind (Tailwind for RN)

---

## 🏗 아키텍처 설계

```
┌─────────────────────────────────────┐
│     모바일 앱 (React Native)         │
│  ┌──────────┐  ┌──────────┐         │
│  │  피드    │  │ 아카이브  │         │
│  │  화면    │  │  화면    │         │
│  └──────────┘  └──────────┘         │
│         │              │             │
│    ┌────┴──────────────┴────┐       │
│    │   상태 관리 (Zustand)   │       │
│    └────────┬───────────────┘       │
└─────────────┼──────────────────────┘
              │
         [Firebase SDK]
              │
┌─────────────┼──────────────────────┐
│         Firebase                    │
│  ┌──────────┴──────────┐           │
│  │   Firestore DB      │           │
│  │  - links collection │           │
│  │  - users collection │           │
│  └─────────────────────┘           │
│                                     │
│  ┌─────────────────────┐           │
│  │  Cloud Functions    │           │
│  │  - fetchLinkPreview │           │
│  └─────────────────────┘           │
│                                     │
│  ┌─────────────────────┐           │
│  │  Storage            │           │
│  │  - 썸네일 이미지     │           │
│  └─────────────────────┘           │
└─────────────────────────────────────┘
```

---

## 📊 데이터 모델

### Firestore Collections

#### `users/{userId}`
```typescript
{
  uid: string;
  email: string;
  createdAt: timestamp;
  settings: {
    defaultView: 'feed' | 'archive';
  }
}
```

#### `links/{linkId}`
```typescript
{
  id: string;
  userId: string;
  url: string;

  // 메타데이터 (Open Graph)
  title: string;
  description: string;
  imageUrl: string;
  siteName: string;

  // 사용자 데이터
  memo: string | null;
  tags: string[];

  // 상태
  isArchived: boolean;

  // 타임스탬프
  createdAt: timestamp;
  updatedAt: timestamp;
  archivedAt: timestamp | null;
}
```

---

## 🎯 주요 기능 명세

### 1. 링크 공유 및 저장
**시나리오:**
1. 사용자가 브라우저/앱에서 "공유하기" 선택
2. StackNScroll 앱 선택
3. 백그라운드에서 링크 메타데이터 추출 (Cloud Function)
4. Firestore에 저장
5. 피드 최상단에 표시

**기술 구현:**
- iOS: Share Extension (Swift)
- Android: Share Intent (Kotlin/Java)
- React Native Share Extension 라이브러리

### 2. 피드 스크롤
**특징:**
- 카드 기반 UI (링크 미리보기)
- 최신 저장 순으로 정렬
- 끝까지 스크롤 시 첫 번째 아이템으로 순환
- 무한 스크롤 (Circular List)

**구현:**
```typescript
// 순환 로직
const getCircularIndex = (index: number, total: number) => {
  return index % total;
};
```

### 3. 스와이프 제스처

#### 오른쪽 스와이프 → 메모 & 태그
- 메모 입력 모달 열림
- 태그 선택/추가 가능
- 저장 시 카드 상단에 메모 표시

#### 왼쪽 스와이프 → 아카이브
- 애니메이션과 함께 카드 사라짐
- `isArchived: true` 업데이트
- 피드에서 제거

**구현:**
- React Native Gesture Handler의 `Swipeable` 컴포넌트
- Reanimated로 부드러운 애니메이션

### 4. 필터링
- 태그별 필터링
- 아카이브된 항목 보기/숨기기
- 검색 기능 (제목, 메모, URL)

---

## 📱 화면 구성

### 1. 피드 화면 (Main Feed)
```
┌─────────────────────────┐
│  StackNScroll      🔍   │  ← 헤더 (검색, 필터)
├─────────────────────────┤
│                         │
│  ┌───────────────────┐  │
│  │ [썸네일 이미지]    │  │
│  ├───────────────────┤  │
│  │ 제목              │  │
│  │ 설명...           │  │
│  │                   │  │
│  │ "내 메모"         │  ← 메모 (있을 경우)
│  │ #태그1 #태그2    │  ← 태그
│  └───────────────────┘  │
│                         │
│  ┌───────────────────┐  │
│  │ [썸네일 이미지]    │  │
│  └───────────────────┘  │
│                         │
│         ⋮               │
│                         │
├─────────────────────────┤
│  [피드] [아카이브]      │  ← 하단 탭
└─────────────────────────┘
```

### 2. 아카이브 화면
- 피드와 동일한 레이아웃
- 아카이브된 항목만 표시
- 스와이프 복원 기능

### 3. 메모/태그 모달
```
┌─────────────────────────┐
│   메모 추가        ✕    │
├─────────────────────────┤
│                         │
│  ┌───────────────────┐  │
│  │ 메모를 입력하세요  │  │
│  │                   │  │
│  └───────────────────┘  │
│                         │
│  태그:                  │
│  [#개발] [#디자인] [+]  │
│                         │
│       [저장]            │
└─────────────────────────┘
```

---

## 🔄 API 설계 (Cloud Functions)

### 1. fetchLinkPreview
```typescript
// Cloud Function
exports.fetchLinkPreview = functions.https.onCall(async (data, context) => {
  const { url } = data;

  // Open Graph 데이터 추출
  const preview = await unfurl(url);

  return {
    title: preview.title,
    description: preview.description,
    imageUrl: preview.open_graph?.images?.[0]?.url,
    siteName: preview.open_graph?.site_name,
  };
});
```

### 2. Firestore Rules
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth.uid == userId;
    }

    match /links/{linkId} {
      allow read: if request.auth.uid == resource.data.userId;
      allow create: if request.auth.uid == request.resource.data.userId;
      allow update, delete: if request.auth.uid == resource.data.userId;
    }
  }
}
```

---

## 📁 프로젝트 폴더 구조

```
stacknscroll/
├── src/
│   ├── components/           # 재사용 가능한 컴포넌트
│   │   ├── LinkCard.tsx     # 링크 카드 컴포넌트
│   │   ├── SwipeableCard.tsx
│   │   └── MemoModal.tsx
│   │
│   ├── screens/             # 화면 컴포넌트
│   │   ├── FeedScreen.tsx
│   │   ├── ArchiveScreen.tsx
│   │   └── AuthScreen.tsx
│   │
│   ├── navigation/          # 네비게이션 설정
│   │   └── AppNavigator.tsx
│   │
│   ├── services/            # Firebase 서비스
│   │   ├── firebase.ts
│   │   ├── auth.ts
│   │   └── firestore.ts
│   │
│   ├── hooks/               # 커스텀 훅
│   │   ├── useLinks.ts
│   │   └── useLinkPreview.ts
│   │
│   ├── store/               # 상태 관리
│   │   └── linkStore.ts
│   │
│   ├── types/               # TypeScript 타입
│   │   └── index.ts
│   │
│   └── utils/               # 유틸리티 함수
│       └── helpers.ts
│
├── functions/               # Firebase Cloud Functions
│   ├── src/
│   │   └── index.ts
│   └── package.json
│
├── ios/                     # iOS 네이티브 코드
│   └── ShareExtension/      # iOS Share Extension
│
├── android/                 # Android 네이티브 코드
│   └── app/src/main/
│       └── ShareActivity/   # Android Share Intent
│
├── App.tsx
├── package.json
└── tsconfig.json
```

---

## 🚀 개발 로드맵

### Phase 1: 기본 기능 (MVP)
- [ ] React Native + Expo 프로젝트 초기화
- [ ] Firebase 설정 및 연동
- [ ] 사용자 인증 (이메일/소셜 로그인)
- [ ] 링크 저장 기능 (수동 입력)
- [ ] 피드 화면 구현
- [ ] 스와이프 아카이브 기능

### Phase 2: 핵심 기능
- [ ] iOS/Android Share Extension
- [ ] 링크 미리보기 자동 생성
- [ ] 메모 추가 기능
- [ ] 태그 시스템
- [ ] 순환 스크롤 구현

### Phase 3: 고급 기능
- [ ] 필터링 및 검색
- [ ] 푸시 알림 (새 링크 저장 시)
- [ ] 오프라인 모드
- [ ] 데이터 내보내기

### Phase 4: 최적화
- [ ] 성능 최적화
- [ ] 애니메이션 개선
- [ ] UX 테스트 및 개선
- [ ] 앱스토어 출시 준비

---

## 💡 주요 고려사항

### 1. 순환 스크롤 구현
- FlatList의 `onEndReached`에서 데이터를 앞에 추가
- 또는 인덱스 계산으로 가상 순환 구현

### 2. 링크 미리보기 성능
- Cloud Function에서 캐싱
- 썸네일 이미지 최적화 (Firebase Storage)

### 3. 오프라인 지원
- Firestore의 오프라인 persistence 활용
- 네트워크 없을 때도 저장된 데이터 표시

### 4. Share Extension 제한사항
- iOS: Swift로 별도 구현 필요
- Android: React Native에서 Intent 처리 가능
- 백그라운드 동기화 고려

---

## 🎨 디자인 가이드라인

### 컬러 팔레트
- Primary: #6366f1 (Indigo)
- Background: #ffffff / #1f2937 (다크모드)
- Card: #f9fafb / #374151
- Text: #111827 / #f9fafb
- Accent: #10b981 (태그, 메모)

### 타이포그래피
- 제목: 16-18px, Semi-bold
- 본문: 14px, Regular
- 캡션: 12px, Regular

### 스페이싱
- 카드 간격: 16px
- 내부 패딩: 16px
- 모서리 반경: 12px

---

## 📝 다음 단계

1. ✅ 기술 스택 선정 완료
2. ✅ 설계 문서 작성 완료
3. ⏭️ React Native + Expo 프로젝트 초기화
4. ⏭️ Firebase 프로젝트 생성 및 설정
5. ⏭️ 기본 UI 컴포넌트 개발 시작
